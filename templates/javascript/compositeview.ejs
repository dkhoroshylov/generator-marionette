define([
    <%= depsList %>
],
function( <%= funcsList %> ) {
    'use strict';

	/* Return a CompositeView class definition */
	return <%= extendFrom %>.extend({

		initialize: function() {
			console.log("initialize a <%=
                _.capitalize(name) %> CompositeView");
		},
		<%=
            itemViewAndTemplateSection %>

    	/* ui selector cache */
    	ui: {},

    	/* where are we appending the items views */
    	itemViewContainer: "",

		/* Ui events hash */
		events: {},

		/* on render callback */
		onRender: function() {}
	});

});
